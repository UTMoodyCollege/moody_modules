<!-- Add you custom twig html here -->
<div class="moody-showcase{{ sticky_image ? ' moody-showcase--sticky-media' }}">
	{% if image %}
		<div class="showcase-image{{ sticky_image ? ' showcase-media--sticky-target' }}">
			{{ image }}
		</div>
	{% endif %}
	{% if video %}
		<div class="showcase-video{{ sticky_image ? ' showcase-media--sticky-target' }}">
			{{ video }}
		</div>
	{% endif %}
	<div class="showcase-text">
		<h3 class="showcase-headline">{{ headline }}</h3>
		<hr>
		<div class="showcase-copy">{{ copy }}</div>
		{{ cta }}
	</div>
</div>
